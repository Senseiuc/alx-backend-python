#!/usr/bin/env python3
"""
a type-annotated function concat
that takes a string str1 and a string str2 as
arguments and concatenate them.
"""


def concat(str1: str, str2: str) -> str:
    """
    adds two strings and returns a string
    """
    return str1 + str2
